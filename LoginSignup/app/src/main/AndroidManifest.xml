<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginSignup"
        tools:targetApi="31">

        <!-- Splash Screen should be the first screen -->
        <activity
            android:name=".IntroSplashLogo"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".SignUpActivity"
            android:exported="false">
        </activity>

        <activity android:name=".UserType"
            android:exported="true">
        </activity>

        <activity android:name=".ResetPasswordActivity"
            android:exported="true">
        </activity>

        <activity android:name=".ForgetPasswordConfirmationActivity"
            android:exported="true">
        </activity>

        <activity android:name=".EmployerFormActivity"
            android:exported="true">
        </activity>

        <activity android:name=".JobSeekerFormActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>
